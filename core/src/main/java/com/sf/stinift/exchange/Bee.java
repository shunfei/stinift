package com.sf.stinift.exchange;

/**
 * The item which being transfered between readers and writers.
 * Since it is a bare interface, anything can be a bee, as long as it can be produced by reader,
 * and can be parsed by writer.
 */
public interface Bee {
}
